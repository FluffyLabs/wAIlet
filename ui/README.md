# wAIlet UI

## dev startup

If you don't have `pnpm` installed:

```console
npm i -g pnpm
```

Install dependencies:

```console
pnpm install
```

Create `.env`:

```console
cp .env.example .env
```

Start dev server:

```console
pnpm run dev
```
